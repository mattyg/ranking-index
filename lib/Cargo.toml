[package]
edition = "2018"
name = "hc_lib_ranking_index"
version = "0.2.0"

[lib]
crate-type = ["cdylib", "rlib"]
name = "hc_lib_ranking_index"

[dependencies]
serde = "1"
hdk = {version = "0.0.163", features = ["encoding"]}
